export type TData = {
  id: number;
  name: string;
  category: string;
  image: string;
  price: string;
};

export type THeadlineData = {
  id: number;
  title: string;
  subtitle: string;
  image: string;
};
